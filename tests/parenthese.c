/*
** EPITECH PROJECT, 2021
** parenthese.c
** File description:
** unit test
*/

#include <criterion/criterion.h>
#include <string.h>
#include <stdlib.h>

#include "my.h"
#include "minishell.h"

/*
Test(parentheses, parcing_1)
{
    int status = 0;
    char *str = my_strdup("ls;ls(cat;cat)ls");
    char *rsl = parcing_parentheses(str, 16, &status);

    cr_assert_str_eq(rsl, "ls;ls;(cat@cat);ls");
    free(rsl);
}

Test(parentheses, parcing_2)
{
    int status = 0;
    char *str = my_strdup("(ls;ls(cat;cat)ls)");
    char *rsl = parcing_parentheses(str, 16, &status);

    cr_assert_str_eq(rsl, ";(ls@ls@(cat@cat)@ls);");
    free(rsl);
}

Test(parentheses, parcing_3)
{
    int status = 0;
    char *str = my_strdup("(ls;ls(cat;cat)ls");
    char *rsl = parcing_parentheses(str, 16, &status);

    cr_assert_str_eq(rsl, ";(ls@ls@(cat@cat)@ls");
    free(rsl);
}

*/
